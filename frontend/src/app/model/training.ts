export class Training {
    constructor (
        public id: number,
        public name: string,
        public description: string,
        public address: string,
        public city: string,
        public postalCode: number
    ) {}
}
